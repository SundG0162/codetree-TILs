#include <iostream>
#include<cmath>
using namespace std;

int main() {
    float a,b,c;
    cin >> a >> b >> c;
    cout.precision(3);
    cout << fixed;
    cout << a << endl << b << endl << c << endl;
    return 0;
}